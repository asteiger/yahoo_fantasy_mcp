# Allowlist only what the image build needs so credentials (oauth2.json,
# .envrc, .mcp.json) and local environments never enter the build context.
*
!pyproject.toml
!uv.lock
!README.md
!LICENSE
!yahoo_fantasy_mcp/
**/__pycache__
